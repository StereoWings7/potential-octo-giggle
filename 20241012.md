次元削減 画像1枚を2次元(グレイスケール)の行列として表現し、この行列に対してSVDを行ない卓越した特異値を数個だけピックアップすることで圧縮する、みたいな事例が軽くネット検索しただけでたくさんでてくるが、これは後々(特にディープラーニングの文脈ではよく)出てくる「多様体仮説」の概念を説明する、という意味ではあまり適切ではないように思う。

SVDではどのような"抽出"が行われているかを探る前に、行列がある空間を"張る"という概念について注釈する。ある$(n,m)$行列$A$があったとして
$$
A= \begin{pmatrix}
\boldsymbol{a_1},\boldsymbol{a_2},\cdots \boldsymbol{a_m}
\end{pmatrix}
$$
ただし$\boldsymbol{a_i}$はn次元ベクトルとする。 このとき、行列$A$はそれを構成する列ベクトル$\boldsymbol{a_i}$によって部分空間を張っている。これらm個のn次元ベクトルによって張られた空間の次元が、行列Aのrank Aとなる。rank Aはある種の部分空間の"太さ"を表現している。
例えば、(3,3)行列$A$の列ベクトル3つが張る空間のrankが1だったら、3つの列ベクトル$(\boldsymbol{a_1},\boldsymbol{a_2},\boldsymbol{a_3})$は全てお互い定数倍の関係にあり、これらが張る部分空間は1次元(単なる直線)になる。もしrankが2だったら、$\boldsymbol{a_3} = c_1 \boldsymbol{a_1} + c_2 \boldsymbol{a_2}$を満たす組$(c_1,c_2)$が存在する、つまりこれら3つのベクトルが張る空間は2次元平面となる。もしrank Aが3だったら、これら3つのベクトルは線形独立、つまりこれら3つのベクトルが張る空間は3次元空間全体に及ぶ。

いわゆる"多様体仮説"は、何らかの意味的なまとまりのある(e.g. CIFAR-10で「飛行機」ラベルがついている)列ベクトルたち$\boldsymbol{a_i}$によって張られた部分空間(これを厳密な数学的定義はおいといてとりあえず"多様体"と呼ぶのだが)の次元(rank A)は、元のベクトルの次元nよりもずっと小さい($rank\ A\ll m$)という主張である。

この「何らかの意味的なまとまりのある列ベクトルたちが張る部分空間(としての多様体)」がどのような形状をしているかを図示するのは、高次元空間の話なのでちょっと難しいけど、斜めにへちゃげた高次元ラグビーボールみたいな感じかと思えばたぶんOK.

SVDは、一般化された行列の対角化と思えばいいので、行列の対角化について考えてみる。ある行列Aの対角化
$$
\Lambda = P^{-1} A P
$$
を考えるとはどういうことをしているかというと、「そのままだと行列Aが張る部分空間は『よじれたラグビーボール』みたいに見えるけど、適切な線形変換$P$を施してやれば、軸方向に揃って見えるようになる」ということをやっている。行列の対角化だとAは正方行列じゃないといけないけど、SVDだと正方行列じゃなくても任意の$(n,m)$行列でも良い、より一般化された形式になっているが、基本的にはやってることは同じ。SVD
$$
A = U\Sigma V^T
$$
においても、変換行列$U,V^T$は対角化のときの$P$とは(お互いがお互いの転置行列になってないという意味では)ちょっと違うけど、意味合いとしては、「元の行列Aが張る空間を、$U,V^T$によって変換している$という点で共通している。

で、この$\Sigma$は対角行列になってて、その対角成分の大きさが「(行列Aの列ベクトルが張る部分空間の)ラグビーボールの長径の大きさ」に対応している。つまり、$U=(\boldsymbol{u_1},\boldsymbol{u_2}\cdots\boldsymbol{u_n})$や$V=(\boldsymbol{v_1},\boldsymbol{v_2}\cdots\boldsymbol{v_n})$がそれぞれのラグビーボールの長径or短径(実際には高次元なので"長"とか"短"以上にもっとたくさんあるけど)の"向き"に対応してて、どっちの向き(その向きは$\boldsymbol{u_1}$だの$\boldsymbol{u_2}$だので定められているけど)について、そのラグビーボールがより"広がっている"かは$\Sigma = diag(\sigma_1,\sigma_2,\cdots \sigma_n)$で表わされる特異値の大きさを見れば良い。多様体仮説は、列ベクトル$\boldsymbol{a}_i$の次元よりも特異値$\sigma_i$が一定以上の大きさを持っている(=ラグビーボールがそれなりに広がりを持っている次元の大きさ)がとても小さい、ということを主張している。特異値$\sigma_i$が十分小さい次元については、ラグビーボールがへちゃげてるのでいっそペタンコだと思って広がりを無視してもそんな誤差はでないはず。 ここから、行列の低ランク近似としてSVDを使ってみる、という考えが生じてくる。

もし1枚の画像(CIFAR-10なら(32,32)ピクセル)をそのまま行列Aだと思ってSVDをかけるなら、このSVDがやってることは、「いちばん左はじのピクセル縦32個(を縦ベクトルとしてみなしたもの)を第1ベクトル$\boldsymbol{a_1}$,その隣のピクセル縦32個(中略)を$\boldsymbol{a_2}$,(以下略)」とみなして、これら32個の32次元ベクトルが張る空間について考えていることとなる。1枚の画像が持つ情報について、それを縦にスダレ状に切り刻んで解析してるようなもので、「多様体仮説」とはちょっと意味合いがズレるけど、それでも元の行列Aを(なるべく元の情報を失うことなく)更なる低次元な空間に埋め込むにはどうすれば良いか?という点では意味合いとしては同じ。